# PostgreSQL Database Configuration
# Copy this file to .env and fill in your actual database credentials

DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password

# Optional: Full connection string (overrides individual settings if provided)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ============================================================================
# API Configuration (for api_server.py)
# ============================================================================

# API Base URL (your integration platform API)
# If running the REST API server locally, use:
API_BASE_URL=http://localhost:3000

# Authentication Method: bearer, api_key, basic, or none
# For local development with rest_api_server.py, use 'none'
API_AUTH_METHOD=none

# Bearer Token Authentication (if your API requires it)
# API_BEARER_TOKEN=your_bearer_token_here

# OR API Key Authentication (if using api_key method)
# API_KEY=your_api_key_here

# OR Basic Authentication (if using basic method)
# API_BASIC_USERNAME=your_username
# API_BASIC_PASSWORD=your_password

# Request timeout in seconds
API_TIMEOUT=30
